import { YoutubeLinkValidatorDirective } from './youtube-link-validator.directive';

describe('YoutubeLinkValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new YoutubeLinkValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
