import { FilesDropDirective } from './files-drop.directive';

describe('FilesDropDirective', () => {
  it('should create an instance', () => {
    const directive = new FilesDropDirective();
    expect(directive).toBeTruthy();
  });
});
